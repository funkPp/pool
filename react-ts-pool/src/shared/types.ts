export interface IUser {
  id: string;
  firstName: string;
  lastName: string;
  userName: string;
  role: string;
  // isDeleting?: boolean;
}

export interface IStudent {
  id: string;
  firstName: string;
  lastName: string;
  birthday: string;
}

